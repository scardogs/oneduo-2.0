/**
 * Batch Upload Constants
 * Shared configuration values for batch upload functionality
 */

// Maximum number of concurrent processing jobs
export const MAX_CONCURRENT_JOBS = 7;

// Default extraction FPS (1 = Fast Mode, 3 = Precision Mode)
export const DEFAULT_EXTRACTION_FPS = 1;

// Session storage key for course agent
export const SESSION_STORAGE_KEY = 'courseagent_session';

// Email storage key
export const EMAIL_STORAGE_KEY = 'courseagent_email';

// Just uploaded storage key
export const JUST_UPLOADED_KEY = 'oneduo_just_uploaded';
